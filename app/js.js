function showPage(page) {
  switch (page) {
    case "home":
        document.getElementsByTagName("body")

        products[] = curl("/api/getProducts.php?category=games&price_top=500");
        products[0].name 
      break;
    default:
      break;
  }
}
